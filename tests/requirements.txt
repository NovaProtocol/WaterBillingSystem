# Test-only dependencies (installed alongside shared/requirements.txt by
# tests/run_tests.sh into tests/.venv). Nothing here is needed by the apps.
pytest>=8
pytest-asyncio>=0.24
aiosqlite>=0.20
uvicorn>=0.30,<1
