# DB
sqlalchemy>=2.0,<3
pymysql>=1.1,<2
python-dateutil>=2.8.0

# fastapi (phase 2+)
fastapi>=0.141,<1
granian>=2.8
httpx>=0.27
aiomysql>=0.2
greenlet>=3.2
python-multipart>=0.0.9

# grpc (internal container-to-container)
grpcio>=1.60,<2
grpcio-tools>=1.60,<2
protobuf>=4,<7

# portals (not Flask — standalone libs; Flask itself is gone)
jinja2>=3.1,<4
itsdangerous>=2.2,<3  # one-deploy JWT fallback only — see shared/wbs_jwt.py
PyJWT>=2,<3  # portals JWT HS256 (reuse SECRET_KEY, ISS=wbs AUD=waterbillingsystem)
cryptography>=42
